MODERATOR HERE BITCH !

User count = <%= @users.count %>
<div class="col-md-offset-2">

<%= will_paginate %>
<% @users.each do |u|%>
<div class="each_user">
	<%= u.name %>	: <span><%= u.email %></span>
</div>
<% end %>
<%= will_paginate %>