<div class="jumbotron">

<h3>Sign up</h3>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
  	<%= f.input :name, required: true, autofocus: true  %>
    <%= f.input :email, required: true %>
    <%= f.input :password, required: true, hint: ("Minimum #{@minimum_password_length} characters required" if @minimum_password_length) %>
    <%= f.input :password_confirmation, required: true %>
     <%= f.input :avatar, as: :file, hint: "should be less than 1 MB" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up" , class: "actual_submit" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>