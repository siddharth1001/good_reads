<h2>
<%= @book.title %>	
</h2>
<h4> Author : <%= @book.author %> </h4>
<%= @book.description %>
<br><br>
<%= link_to "Back", root_path %>

<% if user_signed_in? && @book.user_id == current_user.id%>
<br>
<%= link_to "Edit", edit_book_path(@book) %>
<%= link_to "Delete", book_path(@book), method: :delete, data: { confirm: "Are you suer?"} %>

<% end %>